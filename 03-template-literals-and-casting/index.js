/* eslint-disable prefer-template */
const age = 4;
const ageAsString = '4';

const sarahInfo = '';

// When correcting `sarahInfoAsString` use the variable `ageAsString`
const sarahInfoAsString = '';

// treat this like the numbers excercise by adding `age` to 1
const sarahsAgeNextYear = 0;

// this time, try to add `ageAsString` to 1 and see what happens
const sarahsAgeNextYearAsString = 0;

export {
  sarahInfo,
  sarahInfoAsString,
  sarahsAgeNextYear,
  sarahsAgeNextYearAsString,
};
